const DATA_BASENAME = '@gameplay';

//@app:collection

const COLLECTION_USERS = `${DATA_BASENAME}:users`;
const COLLECTION_APPOINTMENTS = `${DATA_BASENAME}:appointments`;

export {
    COLLECTION_USERS,
    COLLECTION_APPOINTMENTS
}